const fizzBuzz = require("../src/fizzbuzz")

describe("fizzBuzz tests", () => {

test("test 1", () => {
    // this is where we'll write our first failing test
    expect(fizzBuzz(1)).toEqual("");
});
// (slip in the next test here - more on that later)


});
